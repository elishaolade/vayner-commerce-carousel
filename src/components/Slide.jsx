import React from 'react'
import styled from 'styled-components';

export const Slide = (props) => {
    useEffect(() => {
        effect
        return () => {
            cleanup
        }
    }, [input])
    return (
        <div className="Slide">
            
        </div>
    )
}
